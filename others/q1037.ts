// 1037. 有效的回旋镖 https://leetcode-cn.com/problems/valid-boomerang/

function isBoomerang(points: number[][]): boolean {
    function lineOfTwoPoint(one: number[], two: number[]) {
        let [x1, y1] = one;
        let [x2, y2] = two;
    }

    return true;
};